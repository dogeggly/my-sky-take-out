<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <update id="updateSetmeal">
        update setmeal
        <set>
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null and image != ''">image = #{image},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteSetmeals">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <select id="selectSetmealByPage" resultType="com.sky.vo.SetmealVO">
        select s.*, c.name from setmeal s left join category c on s.category_id = c.id
        <where>
            <if test="name != null and name != ''">s.name like concat('%',#{name},'%')</if>
            <if test="categoryId != null">and s.category_id = #{categoryId}</if>
            <if test="status != null">and s.status = #{status}</if>
        </where>
        order by s.update_time desc
    </select>

    <select id="selectSetmealById" resultMap="SetmealVOMap">
        select s.*,
               sd.id sd_id,
               sd.setmeal_id sd_setmeal_id,
               sd.dish_id sd_dish_id,
               sd.name sd_name,
               sd.price sd_price,
               sd.copies sd_copies,
               c.name c_name
        from setmeal s
                 left join setmeal_dish sd on s.id = sd.setmeal_id
                 left join category c on s.category_id = c.id
        where s.id = #{id}
    </select>

    <select id="selectSetmealsByIds" resultType="com.sky.entity.Setmeal">
        select * from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <resultMap id="SetmealVOMap" type="com.sky.vo.SetmealVO">
        <id property="id" column="id"/>
        <result property="categoryId" column="category_id"/>
        <result property="name" column="name"/>
        <result property="price" column="price"/>
        <result property="status" column="status"/>
        <result property="description" column="description"/>
        <result property="image" column="image"/>
        <result property="updateTime" column="update_time"/>
        <result property="categoryName" column="c_name"/>
        <collection property="setmealDishes" ofType="SetmealDish">
            <id property="id" column="sd_id"/>
            <result property="setmealId" column="sd_setmeal_id"/>
            <result property="dishId" column="sd_dish_id"/>
            <result property="name" column="sd_name"/>
            <result property="price" column="sd_price"/>
            <result property="copies" column="sd_copies"/>
        </collection>
    </resultMap>

</mapper>